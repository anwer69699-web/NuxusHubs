local req = (syn and syn.request) or request or http_request
if req then
    local res = req({Url = "رابط_السكربت_هنا", Method = "GET"})
    if res and res.StatusCode == 200 and res.Body then
        local f = loadstring(res.Body)
        if f then f() end
    end
end
